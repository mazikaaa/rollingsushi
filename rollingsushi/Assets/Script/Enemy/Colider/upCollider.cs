using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class upCollider : ColliderBase
{
    // Start is called before the first frame update
    void Start()
    {
        touch = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
